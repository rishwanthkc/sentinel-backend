# Discovered Endpoints

## Summary

- Endpoint Count: 20
- Method Count: 20

## Route List

- GET /
- GET /health
- POST /auth/register
- POST /auth/login
- POST /emergency/trigger
- GET /emergency/history/{email}
- POST /emergency/resolve/{emergency_id}
- GET /emergency/active
- POST /contacts/add
- GET /contacts/{user_email}
- POST /reports/submit
- GET /reports/all
- GET /dashboard/stats
- GET /dashboard/reports
- GET /dashboard/emergencies
- GET /dashboard/users
- GET /dashboard/analytics
- GET /dashboard/hotspots
- POST /dashboard/resolve/{id}
- GET /safety/risk-points
- GET /safety/route

## Notes

This discovery is based on static code inspection. No OpenAPI document was available in the repository.
